# dcore



## API

```javascript
const dcore = require('dcore'),
    $cookie = dcore.cookie,
    $url = dcore.url,
    $element = dcore.element,
    $event = dcore.event,
    $jsonp = dcore.jsonp;

```

### cookie

**Set Cookie**

